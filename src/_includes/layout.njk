<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "Eleventy Tailwind Template" }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;700&family=Lexend+Tera:wght@400;700&family=Roboto+Mono:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ '/assets/styles.generated.css' | url | safe }}">
  </head>
  <body class="bg-gray-50 text-gray-900">
    {% include "navbar.njk" %}
    {{ content | safe }}
    {% include "footer.njk" %}
  </body>
</html>
